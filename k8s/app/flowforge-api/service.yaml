apiVersion: v1
kind: Service
metadata:
  name: flowforge-api
  namespace: flowforge
  labels:
    app: flowforge-api
spec:
  type: ClusterIP
  selector:
    app: flowforge-api
  ports:
    - name: http
      port: 80
      targetPort: 8080
      protocol: TCP
