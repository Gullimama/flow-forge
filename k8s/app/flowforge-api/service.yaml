apiVersion: v1
kind: Service
metadata:
  name: flowforge-api
  namespace: flowforge
  labels:
    app: flowforge-api
spec:
  type: ClusterIP
  selector:
    app: flowforge-api
  ports:
    - port: 8080
      targetPort: 8080
      protocol: TCP
