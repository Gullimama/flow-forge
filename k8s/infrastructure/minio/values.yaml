# Official MinIO chart (minio/minio) - https://charts.min.io/
# Service ports set to 80 for AKS policy (only port 80 allowed for services)
mode: standalone

rootUser: flowforge
rootPassword: flowforge

service:
  port: "80"
consoleService:
  port: "80"

image:
  repository: quay.io/minio/minio
  tag: RELEASE.2024-12-18T13-15-44Z
  pullPolicy: IfNotPresent

persistence:
  enabled: true
  size: 100Gi
  storageClass: managed

# Buckets to create after install (names only; policy defaults)
buckets:
  - name: raw-git
  - name: raw-logs
  - name: parsed-code
  - name: parsed-logs
  - name: graph-artifacts
  - name: research-output
  - name: model-artifacts
  - name: evidence

resources:
  requests:
    cpu: "2"
    memory: 4Gi
  limits:
    cpu: "4"
    memory: 8Gi
