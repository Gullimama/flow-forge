apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: tei-reranker-model-cache
  namespace: flowforge-ml
  labels:
    app.kubernetes.io/name: tei-reranker
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 20Gi
