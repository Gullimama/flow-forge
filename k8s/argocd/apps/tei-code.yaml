apiVersion: v1
kind: ConfigMap
metadata:
  name: flowforge-tei-code-disabled
  namespace: argocd
  annotations:
    flowforge.io/note: "TEI code Application disabled; kept as marker so Argo app-of-apps does not recreate it."
