# Reuse containers when possible (faster runs; requires same JVM)
testcontainers.reuse.enable=true

# Use Docker Hub ryuk image (quay.io/testcontainers/ryuk uses deprecated v1 format)
ryuk.container.image=testcontainers/ryuk:0.11.0
ryuk.container.privileged=true
