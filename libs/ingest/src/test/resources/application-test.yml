spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/flowforge_test
    username: flowforge
    password: flowforge
  jpa:
    hibernate:
      ddl-auto: validate
  flyway:
    enabled: true

flowforge:
  minio:
    enabled: true
  postgres:
    url: jdbc:postgresql://localhost:5432/flowforge_test
    username: flowforge
    password: flowforge
